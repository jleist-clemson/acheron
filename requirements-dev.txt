# Test/dev-only dependencies. Kept out of requirements.txt so they never reach
# the runtime image. Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
# Integration tests (opt-in: `pytest -m integration`, requires Docker)
httpx==0.28.1
asgi-lifespan==2.1.0
testcontainers==4.14.2
